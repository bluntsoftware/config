app:
  name: ${spring.application.name}
server:
  address: localhost
  port: 8081
  use-forward-headers: true
  servlet:
    context-path: /
mongo:
  host: localhost
  port: 27017

management:
  endpoints:
    web:
      exposure:
        include: "*"
